services:
  - type: web
    name: aleensniperbot
    env: python
    buildCommand: pip install --upgrade pip && PIP_ONLY_BINARY=":all:" pip install --only-binary=:all: --no-cache-dir -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
